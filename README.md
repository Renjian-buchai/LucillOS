# LucillOs

Lucilla OS. Also known as the power of going way too far for an inside joke.

## Dependencies

- gcc cross compiler, binutils cross linker to be placed in `~/opt/cross/bin`. Follow OSDev's wiki to see how to compile it yourself
- nasm
- qemu for emulation. Not necessary.

## Building

`make` to build; `make run` to run.
